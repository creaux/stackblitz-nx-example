import { stackblitzNxExample } from './stackblitz-nx-example';
describe('stackblitzNxExample', () => {
  it('should work', () => {
    expect(stackblitzNxExample()).toEqual('stackblitz-nx-example');
  });
});
